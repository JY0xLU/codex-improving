interface:
  display_name: "Dream Consolidate"
  short_description: "Generate and review Dream Loop memory candidates with audit-first consolidation."
  default_prompt: "Use $dream-consolidate to build candidates, review them, and apply audit-first memory updates without rewriting AGENTS.md."
