interface:
  display_name: "Capture Memory"
  short_description: "Record lightweight event observations into the Dream Loop inbox during active work."
  default_prompt: "Use $capture-memory to record a single scoped event into the Dream Loop inbox without consolidating or promoting memory."
